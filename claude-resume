#!/usr/bin/env python3
"""Thin wrapper for running without pip install."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from resume_resume.cli import main

if __name__ == "__main__":
    main()
